import styled from '@emotion/styled';

import { MAX_WIDTH } from '../../styles/constants-v6';

const Container = styled.div({
  margin: '0 auto',
  width: '90%',
  maxWidth: MAX_WIDTH,
});

export default Container;
